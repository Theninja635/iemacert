# Image replacements

The original site's image CDN could not be accessed from this build environment. The image regions in the pages are styled placeholders, with inline comments marking their intended source use.

Add approved IEMA photography or downloaded source assets to this folder, then replace the gradient background definitions in `css/style.css` with the appropriate `url('../images/filename.jpg')` values.
